{
  "name": "jsetimos-typescript",
  "private": true,
  "description": "Build tools for the TypeScript implementation. Run scripts/build_ts.sh (from the repository root) to compile build/jsetimos.js.",
  "scripts": {
    "build": "tsc -p ."
  },
  "devDependencies": {
    "@types/node": "14.0.27",
    "typescript": "3.8.3"
  }
}
