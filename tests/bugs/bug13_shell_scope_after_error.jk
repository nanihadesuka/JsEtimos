// Bug 13: a runtime error inside `do { }` leaves an extra scope pushed
// mode: shell
// expect-output: secret=0
secret = 0
do { for i = 0; i < 1; undefined?fail secret = 5 }
print("secret=${secret}")
