// Bug 9 (BUGS_AUDIT.MD): `?group` crashes on keys like `toString`

groups = (("toString", 1), ("constructor", 2), ("toString", 3))?group
assert(groups?keys == ("toString", "constructor"))
assert(groups?values[0] == (1, 3))
assert(groups?values[1] == (2,))
